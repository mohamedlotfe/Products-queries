var express = require('express');
var router = express.Router();

const TutorialController = require("../core/business/tutorial.controller");
const TagController = require("../core/business/tag.controller");
/* GET users listing. */
router.get('/tt', async (req, res, next) => {

});

module.exports = router;
